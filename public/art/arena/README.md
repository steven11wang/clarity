Optimized fighter-avatar AVIF files live here; high-resolution PNG masters are
kept outside the public bundle in `docs/art/arena-masters/`. Filenames and the
current Midnight Archive brief are in `docs/arena-asset-prompts.md`, and the
exact installed prompt set is in `docs/arena-avatar-generation.md`. Every avatar
remains optional; the Arena draws a glyph fallback when an image is missing.
