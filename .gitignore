node_modules/
dist/
.env
.env.local
.env.*.local
tools/.venv/
tools/__pycache__/
tools/dom-tests/
# The deployed site fetches /data/questions.json and its figures at runtime, so
# those ship with the repo. Everything else in public/data is build scratch.
public/data/*.full.json
public/data/*.sample.json
public/data/parse-report.json
public/data/quarantine.json
public/data/validation-quarantine.json
public/data/validation-report.json
# Third-party article text pulled down by tools/download_college_panda.py.
# Regenerate locally rather than redistributing someone else's writing.
content/
# Pre-redesign snapshot of src/ — git history serves the same purpose.
design-backup-2026-07-31/
.superpowers/
.worktrees/
.claude/
.wrangler/
.DS_Store
